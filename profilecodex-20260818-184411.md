# Codex session profile fallback：Issue #19 复审修复

- 记录时间：2026-08-18T18:44:11+0800
- fix_started_at：2026-08-18T17:58:17+0800
- 任务：GitHub Issue #19 正式双轴 review findings 修复
- Agent：`/root/issue_19_impl`
- 分支 / worktree：`codex/issue-19` / `SimAI-Ascend-worktrees/issue-19`

## Fallback 原因

AGENTS.md 要求任务超过 20 分钟时使用 `profile-codex-session`。当前会话公开的 skill/工具中没有该能力，无法真实调用；本文件是按任务要求生成的 fallback，不冒充 skill 输出。

## 已观察到的耗时来源

1. 六项复审 finding 都要求先用真实 `SimAI_analytical` 公共进程 seam 固定旧实现 RED，再最小 GREEN，不能用内部 validator 或 mock 代替。
2. AICB binding 必须从 Run Manifest 的事后声明迁移到真实 header 与每层 event，并由共享 decoder、`Workload` 和 `Layer` 实际消费，同时保持 legacy 12/13-column 与五类 collective 回归。
3. Model source of truth 从四个声明汇总组替换为 76 项 canonical tensor-type registry；生产需 checked 重建 logical/storage/auxiliary/active scopes，测试需另用 Python 任意精度 oracle 与独立 dtype-byte 表。
4. 四份 artifact 的 exact schema、pre-I/O envelope、不同 byte limit、non-seekable bounded loader、evidenceRef 唯一解析和数字原始词法互相影响；每次 fixture 改动都要依序重算 Model→Step→Routing→Memory→composite→AICB digest 闭包。
5. 每个安全点都要重新进行 61-source 等价链接；最终 80 项 contract suite 全部启动真实进程，单次全回归约 29 秒。

## 当前阶段快照

- clean 基线：`533c5fc909cd13e7b941196834a56d66524036cd`。
- bounded I/O、exact schema、tensor registry、evidence/readiness、真实 AICB event binding、exact numeric lexeme 六项均已完成 RED→GREEN。
- 最近全回归：`Ran 80 tests in 28.883s`，`OK`。
- NPU：未使用；全部为本地 macOS arm64 CPU，无远端机器、无锁等待。
- 阻断：无。构建与结果均限制在 `/private/tmp/simai-issue19-*`，提交前精确清理。

## 后续控制

- 只完成设计同步、严格编译、等价 61-source 链接、独立 oracle、JSON/digest/determinism、敏感信息与 scope-creep 审计。
- 生成单独 fix commit，保持 worktree clean，不 push、不自行启动复审。
