# Codex session profile — Issue #16

- Started: 2026-08-18T11:46:40+08:00
- Profile checkpoint: 2026-08-18T12:07:21+08:00
- Elapsed at checkpoint: 20m41s
- `profile-codex-session` availability: not installed or exposed in this session; this is the required manual fallback record.

## Why the task exceeded 20 minutes

1. Evidence acquisition and repository disambiguation: the first implicit `gh issue view` resolved the Upstream `aliyun/SimAI` repository because both `origin` and `upstream` exist. The request was re-run with explicit `-R Kirrito-k423/SimAI-Ascend` before using #15/#16 as requirements.
2. Local build-tool gap: the macOS environment has Clang but no `cmake`. A clean, equivalent full-source Analytical link command was established from the checked-in CMake source globs instead of weakening build verification.
3. Required vertical TDD: separate red/green cycles covered the minimal Run Manifest, device-selector conflict, fail-closed provenance, and immutable workload digest mismatch.
4. Contract integrity work: Result Manifest SHA-256 values were verified independently with Python `hashlib`; arbitrary paths/logs were kept out of serialized artifacts.
5. Required design delivery: the 4+1 views, Mermaid flow/class diagrams, contract/state propagation, ADR trade-offs, and acceptance mapping were written after implementation so they reflect the final code.

## Avoidable delay and follow-up

- Future multi-remote issue workflows should always pass `gh -R <owner/repo>` explicitly.
- Installing CMake in the standard developer image would allow the repository build script to run directly on macOS; this session did not mutate the host toolchain.
- A shared JSON/schema and digest module can replace the entry-local implementation when more binaries adopt the Shared Run Contract.

No remote credentials were read, no NPU validation was attempted, and no lock wait occurred.
