# Codex session profile fallback：Issue #19

- 记录时间：2026-08-18T17:16:44+0800
- started_at：2026-08-18T16:54:58+0800
- 任务：GitHub Issue #19（10T Target Workload、GTS 与显存契约）
- Agent：`/root/issue_19_impl`
- 分支 / worktree：`codex/issue-19` / `SimAI-Ascend-worktrees/issue-19`

## Fallback 原因

AGENTS.md 要求超过 20 分钟时使用 `profile-codex-session`。当前会话暴露的 skills 列表中没有该 skill，无法真实调用；本文件是按任务指令要求生成的 fallback，不冒充 skill 输出。

## 已观察到的耗时来源

1. 必须完整对齐父 Issue #15、#16–#19 正文/评论、ADR 0001/0002/0004/0005/0006/0007、#16–#18 最终设计和 Shared Run Contract seam，避免 #19 误入 #20 Projected A2A、#21 搜索或 #28 后续能力。
2. 用户要求严格 TDD，最高级 seam 只能是真实 `SimAI_analytical` 进程。每个竖切都需要重新生成内容摘要闭包、进行严格定向编译，并链接与正式 CMake glob/exclude 等价的 61 个源文件，而不是调用内部 validator。
3. Model Manifest 不能由生产公式自证：测试 oracle 使用架构增量算式，生产实现使用冻结 tensor-group 求和，两条独立路径都必须精确得到 `8,414,884,746,526`。
4. Step/GTS 需要分别覆盖 500M 正边界、越界、零/负/非整数/非 JSON-safe 整数、`uint64` 乘法溢出和声明字段不一致，同时保证 Step 失败不抹掉 Model provenance。
5. Target Workload 由 Model、Step、Routing、Memory 四个逐层依赖 artifact 组成；每次输入变异后都必须重新计算下游 digest 和复合 digest，才能让负例只命中目标校验层。

## 当前阶段快照

- 基线确认：HEAD `cad0301b7c50d5a885777d1691d08b4a8a5f0fb9`，初始 worktree clean。
- Model RED→GREEN：完成；独立 oracle 与生产 tensor-group 求和一致。
- Step/GTS RED→GREEN：完成；500M 接受，稳定拒绝码覆盖 limit/factor/overflow/mismatch。
- 第三竖切：正在实现四资源 composite、逐层 provenance/evidence/readiness 与 AICB workload digest binding。
- NPU：未使用；本任务目前全部为本地 macOS arm64 CPU 验证，无锁等待。
- 阻断：无。磁盘起始可用空间约 2.7 GiB，构建产物限制在 `/tmp/simai-issue19-*`。

## 后续控制

- 继续只修改 Issue #19 所有权内的代码、测试、fixture、设计与本 fallback 文件。
- 每个新增外部行为保持真实进程 RED→GREEN；最终执行 46 项上游 contract 回归、完整链接、JSON/digest、敏感信息与 scope-creep 检查。
- 最终 review 按 `code-review` skill 分离 Standards 与 Spec 两个轴；发现项修复后再提交。
