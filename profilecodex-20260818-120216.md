# Codex 会话耗时剖析（fallback）

- **生成时间：** 2026-08-18T12:02:16+08:00
- **覆盖任务：** SimAI-Ascend #16–#30 Frontier Execution Goal
- **触发原因：** 顶层任务持续超过 20 分钟。
- **工具状态：** 项目要求的 `profile-codex-session` 不在当前可用技能清单中，且本机 `command -v profile-codex-session` 无结果。本报告是基于已落盘过程记录和 Git/Agent 状态的只读 fallback，不冒充该工具输出。

## 当前结论

耗时来自任务本身的强制基线与真实构建门禁，而非无信息重试：先完整读取 Goal、仓库规则、父规格、15 张 Issue、评论、CONTEXT、13 份 ADR 与三个执行技能；随后获取最新 GitHub 状态、解析依赖并确认唯一 frontier #16；创建 Goal 合同、独立 worktree 与全新实施 Agent；#16 先通过真实旧入口建立构建基线，再进入逐切片 TDD。

## 已观测时间线

| 时间 | 阶段 | 可观察证据 |
|---|---|---|
| 11:41–11:43 | BASELINE | `goal_process/simai-ascend-frontier-execution/runs/20260818-C001/ITERATION.md` |
| 11:43 后 | #16 基线 | `codex/issue-16` worktree 基于 `origin/main@d928140`；本机无 CMake，Agent 使用等价 clang++ 真实全量编译入口 |
| RED | contract seam | 真实进程将 `--run-manifest` 视为未知参数，exit 255，Result Manifest 不存在 |
| GREEN 进行中 | 最小实现 | `AnalyticalAstra.cc` 与新 `RunContract.cc/.h` 已修改；真实可执行和脱敏运行结果已产生 |

## 主要耗时因素

1. **规格体量大但必要：** 父 Issue #15 包含 98 条 User Story、Implementation/Testing Decisions；15 张子票与 13 份 ADR 共同限定 seam、依赖和 fail-closed 语义。
2. **依赖计算必须以实时状态为准：** 不能用 `ready-for-agent` 标签代替 blocker CLOSED 检查；首轮确认只有 #16 可执行，避免错误并行。
3. **真实二进制门禁：** #16 要求从真实 `SimAI_analytical` 进程边界验证，而不是只写 schema 单测；本机缺少 CMake 后仍需找到并验证等价全量编译路径。
4. **TDD 串行切片：** legacy manifest GREEN 后才新增冲突参数 RED；这有意牺牲一次性批量修改速度，换取公开行为证据。
5. **强制交付门槛尚未开始：** 实现后仍需 4+1 设计文档、双轴独立 review、修复、与最新 main 集成、最终验收、push、permalink、评论和关闭。

## 非耗时原因

- 未运行 NPU，没有锁等待。
- 未进行无新证据的重复实验。
- 未发生 merge conflict 或权限阻断。
- 未启用费用监控或人民币/API 费用计算。

## 控制措施

- 保持每轮单一 micro-goal，并在 frontier 变化后重算依赖。
- 实现 Agent 只负责 #16；两名 review Agent 槽位保留到实现提交后。
- 临时 build、`__pycache__` 和运行 CSV 不进入提交，除非它们是明确脱敏交付件。
- 若同一编译/测试错误无新证据重复超过一次，停止该路径并固化最小复现或升级。

## 下一检查点

#16 本地 commit READY_FOR_REVIEW；届时固定 `origin/main...codex/issue-16` diff，启动 Standards/Spec 两轴审查。
