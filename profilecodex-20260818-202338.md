# Codex Session Profile Fallback — Issue #21

- `started_at`: `2026-08-18T19:57:18+0800`
- `profiled_at`: `2026-08-18T20:23:38+0800`
- `profile_tool`: `profile-codex-session` 不在当前可用 skill/工具清单中；本文为真实 fallback，未伪造工具输出。
- `scope`: Issue #21 A2 Ground Truth 与校准闭环实施。

## 耗时原因

1. 强制上下文较大：完整读取父 #15、#18/#19/#21、CONTEXT、ADR、前置设计与 TDD 指令，并先建立 61-source 链接、84 项真实进程基线。
2. 真实 A2 前置审计必须脱敏并让整段远端命令持 `flock`；两条 A2 记录中一条认证失败，可达环境又存在 Python/TorchNPU/CANN/HCCL/source pin 缺口。
3. 为穷尽安全诊断，检查了目标 conda env、已有源码 mirror、wheel/cache、CANN/HCCL 与官方包源。一次逐环境只读导入因某环境挂起而被安全中止，并用锁探针确认释放。
4. 本地实现必须从公共真实进程 seam 执行 RED→GREEN，并同时保持内容寻址、统计、异常分类、DerivedCostModel raw digest 闭包与 #16–#19 回归。

## 当前结论

- Remote readiness 保持 `BLOCKED_ENV`；未运行 NPU 训练或采样，未生成或提交伪 Ground Truth。
- synthetic fixtures 只验证 contract/statistics/model reload，不会把远端 readiness 或证据升级为真实 `MEASURED/READY`。
- 首个真实进程 RED 为 Result 缺少 `results.a2_ground_truth`；新增 RunContract 编译已通过，正在完成首个 GREEN 链接与进程验证。
