# Codex Session Profile Fallback — Issue #17 Rereview Fix

- started_at: `2026-08-18T13:41:08+08:00`
- checkpoint_at: `2026-08-18T14:05:52+08:00`
- elapsed_at_checkpoint: `24m44s`
- profiler: `profile-codex-session` unavailable; manual repository-safe fallback

## Why this fix exceeded 20 minutes

1. Five independent review findings required public-process RED→GREEN evidence, including signed rounding overflow, canonical units/formula/evidence conflicts, structured Profile field evidence, and RawObservation/model numerical consistency.
2. Profile fixture structure changes intentionally invalidated the Profile → RawObservation → DerivedCostModel → Run Manifest SHA-256 closure, so every dependent digest was recalculated and verified.
3. The original monolithic resource routine was safely refactored only after behavior became green, extracting one shared artifact loader and three typed validators without changing the public Run/Result Manifest seam.
4. Each behavioral slice required relinking the real `SimAI_analytical` binary because the repository has no local CMake executable.

## Scope and environment

- CPU-only local validation; no NPU, remote host, credentials, or lock wait.
- No Issue #18 collectives were added.
- Build/test artifacts are quarantined under explicit `/tmp/simai-issue17-*` paths before commit.
