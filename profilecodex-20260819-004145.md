# Codex Session Profile — Issue #24

## 时间

- 开始：`2026-08-19T00:12:26+0800`
- 触发 fallback：`2026-08-19T00:41:45+0800`
- 触发时 wall-clock：`29m19s`
- 结束：`2026-08-19T01:04:15+0800`
- 总 wall-clock：`51m49s`
- `profile-codex-session`：当前可用 skill 清单中不存在，因此使用本文件记录真实耗时原因。

## 耗时构成

1. 强制上下文：完整读取 TDD/code-review skill、仓库 AGENTS/CONTEXT、Issue #15/#19/#20/#21/#24 全部正文与评论、相关 ADR 和 issue-19/20/21 设计。
2. 基线门禁：在生产代码前按正式 CMake glob/exclude 集合做 63-source C++11 全链接，并跑 114 个真实 `SimAI_analytical` 进程测试。
3. 进程边界 TDD：从 Run Manifest 到 Result Manifest 建立 topology-placement RED，再逐项验证 topology identity/evidence、资源情景、独立枚举 oracle 和成对候选。
4. 编译成本：每个 ABI 安全点都做完整链接；加入 #24 typed seam 后集合为 64 个源文件。
5. 大规模计算：每个 98,304/100,000-rank 候选都由 #20 `ProjectedA2A` typed seam 聚合域矩阵，独立 oracle 逐 rank/逐 group 复算，不用 O(P^2) resident state。
6. 合同硬化：复用 #19 内容寻址与 target workload 绑定，校验 exact keys、regular file、256 KiB 上限、拓扑和 ragged capability evidence/readiness。

## 非耗时原因

- 未使用远端、NPU、flock 或网络；无设备/认证等待。
- 未启动 reviewer、subagent 或其他并行 Agent。
- 未执行 push、Issue 评论或状态修改。
- token：N/A；平台未提供可可靠归属的单一 token 快照，未估算。

## 后续控制

- 后续只在新增负向合同、100k 实进程预算和最终审查门禁处做风险相称验证。
- 最终 64-source C++11 link PASS（16.79 s，RSS 210,452,480 B，binary SHA-256
  `fb022f5d009db8c7ac15bac3933474c0e4889e58b3f4dba661687c73367daed8`）；strict
  objects `2/2` PASS；真实进程回归 `122/122` PASS（53.357 s，RSS 126,877,696 B）。
- 100,000-rank exact traffic-only 单进程 PASS（wall 0.838377 s；外部 RSS
  33,636,352 B；Result 705,247 B）；independent oracle、determinism、JSON、digest、
  fail-closed、敏感信息和 scope 门禁全部 PASS。
