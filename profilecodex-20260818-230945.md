# Codex Session Profile — Issue #20

## 时间

- 开始：`2026-08-18T22:42:06+0800`
- 触发 fallback：`2026-08-18T23:09:45+0800`
- 触发时 wall-clock：`27m39s`
- 结束：`2026-08-18T23:26:23+0800`
- 总 wall-clock：`44m17s`
- `profile-codex-session`：当前可用 skill 清单中不存在，因此使用本文件记录真实耗时原因。

## 耗时构成

1. 强制上下文：完整读取 TDD skill、仓库 AGENTS/CONTEXT、Issue #15/#18/#19/#20/#21、ADR-0005/0006/0007 与相邻设计。
2. 基线门禁：先做 CMake glob/exclude 等价 62-source C++11 全链接，再跑 108 个真实 `SimAI_analytical` 进程测试；未在基线完成前写 #20 生产代码。
3. TDD vertical slices：uniform、locality、hotspot、resident-state honesty、100k closed-form、fail-closed artifact binding 逐项记录公共进程 RED，再做最小 GREEN。
4. 编译成本：本机无 CMake，每个 provider/Result ABI 安全点都需重新执行 63-source C++11 全链接，单次约 15 秒。
5. 大规模证据：100,000-rank 测试运行真实进程并输出 100,000 个 per-rank 摘要与 10,000 个 domain cells；同时记录 wall、RSS、Result 大小上限与状态条目计数。
6. 合同硬化：新 schema 与 #19 routing v1alpha1 分离；验证内容摘要、regular-file gate、8 MiB 上限、exact keys、membership、resource identity、evidence/readiness 与 dense HCCL counts 逐 cell 绑定。

## 非耗时原因

- 未使用远端、NPU、flock 或网络；无设备/认证等待。
- 未启动 reviewer、subagent 或其他并行 Agent。
- 未执行 push、Issue 评论或状态修改。
- token：N/A；平台未提供可可靠归属的单一 token 快照，未估算。

## 后续控制

- 只保留风险成比例的全链接/全回归，不重复无信息的编译。
- 设计、fixtures 与最终验证证据在实现收敛后一次补齐。
- 最终 63-source C++11 link PASS（14.77 s，RSS 214,106,112 B）；strict objects `3/3` PASS；真实进程回归 `113/113` PASS（39.609 s，RSS 124,223,488 B）。
- 100,000-rank 真实进程 PASS（wall 0.27508245781064034 s；外部 RSS 75,218,944 B；Result 7,892,501 B；resident state 110,002）。
- 独立 oracle、JSON fixture、determinism/conservation、敏感信息/scope 与 `git diff --check` 门禁全部 PASS。
