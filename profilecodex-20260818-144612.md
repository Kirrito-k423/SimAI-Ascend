# Codex Session Profile Fallback — Issue #18

- started_at: `2026-08-18T14:25:52+08:00`
- checkpoint_at: `2026-08-18T14:46:12+08:00`
- elapsed_at_checkpoint: `20m20s`
- profiler: `profile-codex-session` is neither an available skill nor an installed command; manual repository-safe fallback

## Why this implementation exceeded 20 minutes

1. Required evidence reading covered the complete parent #15 specification, #16/#17/#18 issue bodies and comments, all repository ADRs, the final #16/#17 designs, and the TDD/code-review instructions before changing the public contract.
2. The only accepted product seam is a freshly linked real `SimAI_analytical` process. Each vertical slice therefore requires a stable RED, a 61-source CMake-equivalent link, and Result Manifest assertions rather than internal unit shortcuts.
3. Five collective payload/traffic contracts must remain distinguishable while preserving all #16/#17 external regressions. AR, AG, RS, uniform A2A and routed A2AV are being added one observable behavior at a time.
4. The local macOS arm64 environment has Clang but no CMake. Builds use the repository CMake glob/exclude source set exactly and keep temporary binaries outside the worktree.
5. A2AV needs an immutable routing-count artifact, byte-conservation checks, digest binding, and distinct missing-routing readiness without implementing the #19 Target Workload routing contract or the #28 Simulation flow provider.
6. The data volume reached the local filesystem capacity limit during the A2AV slice. Only this task's three explicit temporary build directories and generated Python bytecode were deleted; source and user data were not touched.

## Scope and environment

- CPU-only local validation; no remote configuration was read and no NPU command or NPU lock was used.
- `implement` and `profile-codex-session` skills are unavailable; neither is claimed as used.
- Work remains limited to Issue #18 Analytical collective costs, contract tests, sanitized fixtures, and design documentation.
