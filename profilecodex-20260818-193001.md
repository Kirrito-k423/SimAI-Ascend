# Codex Session Profile — Issue #19 fix2

- 时间窗口：2026-08-18 19:05:42–19:30:01（Asia/Shanghai，记录时已用时 24 分 19 秒）
- 触发原因：本轮 Standards 复审修复超过 AGENTS.md 规定的 20 分钟。
- 工具状态：已执行 `command -v profile-codex-session`，本机 PATH 中不可用；本文是要求的真实 fallback，不声称运行了缺失工具。

## 耗时来源

1. Workload identity finding 要求真实进程、无 `sleep` 且可重复的竞态 RED。测试使用 Model FIFO 的 reader/writer open 握手作为确定性同步点，在摘要校验与后续资源读取之间执行 workload path 的原子替换；旧实现稳定执行替换后的 bytes，同时 Result 保留旧摘要。
2. 初始 18 列正例选择了不适合的 HCCL timing 观察面：旧实现与最小修复都返回 `HCCL_MODEL_DOMAIN_MISS`。没有把该失败冒充 GREEN，而是改为在 Result 中回读真实 `Layer::specific_parallellism`，直接证明 runtime 是否消费第 13 列。
3. 每个生产小步都需重新链接与正式 CMake glob/exclude 等价的 61 个 C++11 源；最终 83 项 contract suite 全部启动真实 `SimAI_analytical` 子进程，单轮约 29 秒。
4. 收口同时执行变更编译单元的严格 warning gate、独立 registry/GTS/HBM/digest oracle、设计同步和 scope/sensitive 扫描。

## 结果

- RunContract 首读的不可变 workload byte snapshot 贯穿 SHA-256、composition/AICB 校验和 `Sys -> Workload` 运行时解析；legacy CLI 未注入 snapshot 时保持原路径读取。
- target-bound `HYBRID_CUSTOMIZED` 18 列通过统一 customized record-format 判定设置具体并行策略，并从真实 Layer 逐层写入 runtime attestation；缺列 fail closed。
- CPU-only；没有执行 NPU、CANN、远端命令或等待设备锁。
