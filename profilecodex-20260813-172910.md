# Codex Session Profile — 2026-08-13 17:29:10 +08:00

## 范围与方法

本文件响应仓库 AGENTS.md 对超过 20 分钟连续任务的耗时分析要求。当前可用技能目录与会话技能清单中均未发现 `profile-codex-session`，因此没有假称调用该技能；以下是明确标注的人工回退分析。未读取或记录私有机器地址、账号、凭据和原始远程日志。

## 主要耗时来源

1. **长链路决策依赖。** 目标横跨 Upstream SimAI 基线、A2/A3 证据、Profile/HCCL、10T workload、2048-EP traffic、A5 敏感性、100k placement、Top-5、goodput 与 Simulation。Wayfinder 每轮只关闭一个非研究票，减少了决策串扰，但增加了跨轮交接次数。
2. **证据身份必须分离。** 1,024 current-product、8,192 architecture limit、A2/A3 measured/held-out 与 A5 estimated 不能混用，导致每个结论都需要 provenance、readiness 和 UNKNOWN/fail-closed 审核。
3. **HITL 边界较多。** 用户要求每次五项批量确认，速度已优于逐项询问，但每个前沿仍需 recon、决策包、确认、ADR、Goal 账本、Issue 和地图闭环。
4. **仓库远端存在歧义。** 本地同时配置 `origin=Kirrito-k423/SimAI-Ascend` 与 `upstream=aliyun/SimAI`。一次未显式指定 `-R` 的只读 Issue 查询命中了上游同号票据；未产生写操作，但造成额外核对。后续 GitHub 操作必须显式指定目标仓库。
5. **公开仓脱敏与可复现双门禁。** 每次更新需同时检查绝对私有路径、远程身份、凭据、Markdown、JSON、Git 差异和远端 Issue 状态，属于必要的发布成本。

## 已采取的提速措施

- 保持一次五项 HITL，并在用户“全部接受”后一次性写 ADR、领域词汇和 Goal 证据。
- GitHub 命令固定显式使用 `-R Kirrito-k423/SimAI-Ascend`，消除同号上游 Issue 歧义。
- 将 Top-5 搜索本身分为 F0–F3，并把 refined candidates 限制为每切片最多 100，避免把规划阶段扩成 100k packet simulation。
- goodput 和 Simulation smoke 保持独立票据；本轮不提前展开第二前沿。
- 继续使用内容寻址、稳定 rejection code 和 traffic-only 隔离，减少后续反复解释无完整时间模型的候选。

## 下一轮建议

下一轮只处理 #10：以五项批量冻结故障域、MTBF/恢复/checkpoint/spare/ragged communicator 的符号场景，直接复用 ADR-0011 的 goodput `UNKNOWN` 消费口，避免重新讨论 Top-5 排名。完成后再单独处理 #14 Simulation smoke。
