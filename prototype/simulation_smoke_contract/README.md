# PROTOTYPE — Simulation smoke contract

Question: does a 16-rank, two-domain smoke matrix distinguish “the Ascend Simulation path is connected” from “Simulation timing agrees closely enough to audit a candidate family”, while keeping failure states fail-closed?

This is throwaway decision support. It does not run NS-3, model Ascend 950DT, or belong on `main`.

Run:

```bash
python3 prototype/simulation_smoke_contract/tui.py
```
