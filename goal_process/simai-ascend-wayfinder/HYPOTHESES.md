# 假设账本

| ID | 可证伪假设 | 等级 | 支持证据 | 反证/替代解释 | 下一判别动作 | 状态 |
|---|---|---|---|---|---|---|
| H-01 | 私有清单中至少一台 A2 和一台 A3 当前可通过密钥登录并完成只读盘点 | E2 | run C001：两台 A2 与一台 A3 均成功完成有界只读探测 | 仅代表采集时刻可达，不承诺持续可用 | 后续每个实验仍做单次 preflight | SUPPORTED |
| H-02 | A2/A3 至少有一种共同的 Ascend 训练或测量栈可支撑后续 Ground Truth slice | E1 | MindSpeed-LLM 26.1 官方表同时兼容 TorchNPU 7.3/26.1 与 CANN 8.5.X/9.1.X，且安装指南明确支持 A2/A3 | 具体 driver、wheel、ABI、16-rank 建域仍未现场验证 | 在两套隔离环境执行 L0；任一失败即停止 | SUPPORTED |
| H-03 | A2/A3 无需共享同一 wheel 集，只要固定共同源码/配置/指标契约和各自官方兼容环境即可形成可比较 Ground Truth | E1 | 固定 MindSpeed-LLM 26.1、MindSpeed Core 26.1、Megatron core_v0.12.1、shape manifest 与指标 schema；底层按官方兼容矩阵分 lane | 跨代算子/collective 实现差异仍可能超过 30%，必须留出验证 | A2 拟合后冻结参数，仅在 A3 重放并报告误差 | SUPPORTED |
| H-04 | 一个现有 Ascend Megatron/MindSpeed 训练栈加官方通信基准可覆盖 GEMM/MoE、AR/AG/RS/A2A 和端到端 step | E1 | 官方 DeepSeek-V3 ST 覆盖真实训练/Grouped GEMM/MoE/step；HCCL Test 官方支持 AR/AG/RS/A2A 及正确性/性能字段 | 具体产品上的工具可用性、消息曲线和端到端 trace 均未实测 | 按 L0→L1→L2→L3 门禁采集，失败不外推 | SUPPORTED |
| H-05 | 一套 provenance-first schema 可同时表达 A2/A3 实测值与无真机 A5/950DT 的厂商规格、用户输入和派生区间，而不混淆证据身份 | E1 | 固定 Upstream 源码与官方接口支持三层资源；证据类别和现场 readiness 正交；A2/A3/A5 脱敏骨架通过 YAML 解析和不变量审计 | 真实设备字段仍可能要求代际 extension；当前 schema 只承诺最小共同核心 | 在 Provider seam 与首轮 L0/L1 采集中以 validator 验证扩展点，不兼容即升级 schema 版本 | SUPPORTED |
| H-06 | HCCL raw samples、显式消息语义/拓扑/软件身份与独立 derived fit 足以替代 legacy busbw 标量，并保持 GPU 路径向后兼容 | E1 | HCCL Test 原生时间/算法带宽、C API 消息语义和 SimAI 消费点已映射；raw immutable、fit 独立、A2AV 外置 artifact、legacy adapter fail-closed | overlap 与极端非均匀路由仍需 L3/目标 workload 验证，不能由 collective curve 自动推出 | Provider seam 原型先证明 Ascend 输入显式选择且 GPU legacy 回归不变；实测后只在精确 domain 内拟合 | SUPPORTED |
| H-07 | 在 `Layer::compute_time()` 前增加可选 `CollectiveCostModel` dispatch，null/legacy 分支保留原函数，足以接入 Ascend Analytical 而不改 workload schema | E2 | prototype `3f31ca1` 完成 `Sys` 默认 null 注入和 `Layer` 单一 dispatch；6/6 场景通过；Analytical 完整链接且旧构造调用点无需修改 | overlap、global `UserParam` 或对象生命周期仍需正式实现与真实 workload 验证 | `/to-spec` 后以 production 类型重做并加入 legacy 回归 | SUPPORTED |
| H-08 | `AcceleratorProfile` 只负责事实与引用，provider resolver 可在入口显式选择 legacy NVIDIA 或 Ascend HCCL，并对 profile/GPU 冲突 fail-closed | E2 | resolver 对无 profile 保留 legacy，对 Ascend profile 选 HCCL，对 profile+legacy GPU 返回 `CONFLICT`；用户 HITL 接受 | 两 backend 当前 parser 不同，正式 CLI 仍需分入口接线，不能假装已经统一 | 正式实现分别接线并共享 resolver 契约 | SUPPORTED |
| H-09 | Ascend Analytical cost model 与 Simulation flow provider 必须分离；首版 Ascend+Simulation 明确 unsupported 比复用 `MockNcclGroup` 更安全 | E2 | Ascend+Simulation 缺 flow 时返回 `UNSUPPORTED_BACKEND`，future flow capability 可独立开启；Simulation 核心目标编译成功；用户 HITL 接受 | production provider 可聚合两个子接口，但 capability 与失败语义仍须分离 | 后续 Simulation smoke 票定义最小 HCCL flow 验收 | SUPPORTED |
| H-10 | 从固定官方 safetensors header 提取 compact tensor templates，可逐 tensor 复现 V4-Pro baseline，并只将 routed experts/TopK 改为 2048/16 得到唯一目标计数 | E2 | C005：64 shard、145,116 tensor 的名称/dtype/storage shape 全量一致；baseline aggregate PASS；target 逐 tensor与独立算术均为 8,414,884,746,526；用户接受全局 E/K 作用于 61 main+1 MTP | scale、hash table、MTP 或训练 dtype 若混入同一口径会产生不同数字 | `/to-spec` 时转成 production manifest validator | SUPPORTED |
| H-11 | Workload Schema 将模型/tensor manifest、step/GTS、routing evidence 和 memory events 分层引用，足以保留 SimAI workload provenance 而不把硬件/并行假设写进模型身份 | E2 | C005：7/7 场景；改变 GTS、routing、memory completeness 时 model identity 稳定；501M step INVALID 但 model manifest 保留；用户接受四层资源与 content-hash adapter | 单一大 YAML 更直观，但会把可复用模型语义与配置搜索实例耦合 | `/to-spec` 时定义 schema version 和 adapter validation | SUPPORTED |
| H-12 | `active_params_per_token` 只表示一次 token 前向触达的逻辑模型参数；checkpoint bytes、optimizer state、峰值显存和 useful tokens 必须独立报告 | E2 | C005：三种 active scope、checkpoint dtype/storage、500M GTS 与 token accounting 分离；缺 placement/optimizer/runtime 时 peak HBM 保持 null；用户接受 fail-closed 语义 | 用单一 param count 或 bytes/param 会更短，但会错误驱动显存和 MFU | 后续 placement 票只消费带 scope 的字段 | SUPPORTED |
| H-13 | A2A/A2AV 可流式投影成逐 rank 收发、域对矩阵与拓扑资源负载，而无需保留显式 pair flows，并在小规模对确定性 Analytical contention functional 保持流量与时间完全守恒 | E2 | C006：真实 Upstream P=4 probe PASS；hierarchical 在 uniform/hotspot/locality/ragged 的六项守恒 4/4；uniform 闭式与 exact 等价；2048 summary 8,200 cells；用户接受 representation、复杂度和 artifact 边界 | 对称折叠只在 block symmetry 等价；代表流除 uniform 外失败；arbitrary dense A2AV 读取仍 O(P²) | `/to-spec` 时定义 production schema 与 validator | SUPPORTED |
| H-14 | Hierarchical traffic projection 只扩展 Analytical cost-model input，不能冒充 HCCL Simulation flow provider；缺 topology path、routing artifact 或匹配的 HCCL cost model 时必须 UNKNOWN | E2 | C006：缺 routing/topology/cost 分别输出三种 UNKNOWN；aggregate simulation 永远 unsupported；合成时间显式非 ns/非校准；用户接受 fail-closed 与 Simulation 隔离 | 将 aggregate flow 注入 NS-3 更省事，但会伪造端点、并发和拥塞 | Simulation smoke 票实现独立 HCCL flow provider | SUPPORTED |
| H-15 | 三组结构同态但跨 EP/DP 代际的 A2 校准/A3 严格留出 case，足以判断首个 Ascend Vertical Slice 是否达到逐 case≤30%的学习阶段门槛 | E1 | 固定 Ground Truth stack 与 GT-TARGET 语义；C007 用户接受 balanced/communication/long 三组、严格揭盲和逐 case 门槛 | 尚无真实 L0–L3 或预测/实测结果；小留出集不能证明 A5/100k 精度 | `/to-spec` 后先冻结预测，再执行 A2 L0–L3 与 A3 留出 | OPEN |
| H-16 | 将环境阻塞、无效执行与有效精度结果分开，并在高变异时用 10 次 P90，可避免把不可运行或噪声结果误判为模型精度 | E1 | 既有 raw observation schema 固定 CV/type7 P90；C007 用户接受 5次 median/10次 P90、INVALID/BLOCKED 与复验规则 | P90 在仅 10 个样本时仍是粗略尾部估计；只能满足探索门槛 | 实施时以 validator 和确定性统计单元测试证明状态互斥与公式一致 | SUPPORTED |
