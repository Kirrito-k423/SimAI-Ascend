用户已一次性接受 Fault Goodput 的五项决策：committed useful tokens/总墙钟定义；按拓扑故障域显式输入且不默认独立；固定 no-failure/rollback/hot-spare/ragged/correlated-loss 五策略；完整 checkpoint/spare/ragged 语义；common traces 先5次、CV>10%扩10次并按不利P10 goodput。

没有 A5 MTBF/恢复/checkpoint 数字时只输出符号响应面和 break-even，最高 fault goodput 保持 UNKNOWN，不制造 nominal champion。证据：ADR-0012、run C011 与最终 `/to-spec` 输入。
