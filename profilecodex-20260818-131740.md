# Codex session profile — Issue #17

- Started: 2026-08-18T12:55:58+08:00
- Profile checkpoint: 2026-08-18T13:17:40+08:00
- Elapsed at checkpoint: 21m42s
- `profile-codex-session` availability: not installed or exposed in this session; this is the required manual fallback record.

## Why the task exceeded 20 minutes

1. Required evidence reading: the implementation was gated on the full parent #15 specification, #16 final design/implementation, ADR 0001/0003/0005, Profile/HCCL research, and the TDD/code-review instructions.
2. Repository disambiguation: implicit `gh issue view` resolved the Upstream `aliyun/SimAI` repository because the worktree has both `origin` and `upstream`; all authoritative issue reads were repeated explicitly against `Kirrito-k423/SimAI-Ascend`.
3. Local build-tool gap: this macOS environment has Clang but no CMake. Baseline and green verification therefore use a clean equivalent full-source Analytical link derived from the checked-in CMake glob/exclude rules.
4. Required vertical TDD: separate process-boundary slices covered the valid 4-rank HCCL AllReduce, missing Profile, missing cost model, and runtime model-domain miss while retaining all eight #16 regressions.
5. Contract integrity: Profile, immutable RawObservation, and DerivedCostModel artifacts are independently hashed and validated for schema, units, evidence/readiness, exact rank/group/topology/message domain, formula, traffic semantics, and no-extrapolation behavior.

## Avoidable delay and follow-up

- Future multi-remote issue reads should always pass `--repo Kirrito-k423/SimAI-Ascend` on the first command.
- A standard developer image with CMake would let the checked-in build wrapper run directly.
- The legacy process expects an ignored `results/` directory; test setup should eventually create an isolated output directory instead of relying on workspace state.

No remote configuration or credentials were read, no NPU verification was attempted, and no NPU lock wait occurred.
